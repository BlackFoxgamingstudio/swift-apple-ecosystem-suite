[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sovereign-swift-apple-ecosystem-suite"
version = "1.0.0"
description = "Native macOS & iOS SwiftUI Offline-First Architecture Suite"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Russell Alan Powers", email = "russell@sovereignbizbox.io"}]

[project.scripts]
sbb-apple = "src.cli:main"
